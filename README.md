# 🔒 Spring Security Demo Project

Демонстрационный проект Spring Boot, полностью сфокусированный на Spring Security. Этот проект демонстрирует все основные аспекты безопасности в Spring приложениях.

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Основные концепции Spring Security](#основные-концепции-spring-security)
3. [Архитектура проекта](#архитектура-проекта)
4. [Компоненты и их назначение](#компоненты-и-их-назначение)
5. [Методы аутентификации](#методы-аутентификации)
6. [JWT токены](#jwt-токены)
7. [Авторизация](#авторизация)
8. [Запуск проекта](#запуск-проекта)
9. [Тестовые учетные записи](#тестовые-учетные-записи)
10. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## 🎯 Обзор проекта

Этот проект создан для демонстрации и изучения Spring Security. Он включает:

- ✅ Аутентификацию пользователей (in-memory и через БД)
- ✅ **JWT токены для stateless аутентификации**
- ✅ Авторизацию на основе ролей (Role-Based Access Control)
- ✅ Защиту URL и методов
- ✅ Кодирование паролей (BCrypt)
- ✅ Управление сессиями
- ✅ CSRF защиту
- ✅ Custom UserDetailsService
- ✅ Метод-уровневую безопасность
- ✅ REST API защиту

---

## 🧠 Основные концепции Spring Security

### 1. Аутентификация (Authentication)

**Аутентификация** - это процесс проверки личности пользователя (кто вы?).

#### Компоненты аутентификации:

- **UserDetailsService** - интерфейс для загрузки пользовательских данных
- **AuthenticationProvider** - провайдер для проверки учетных данных
- **PasswordEncoder** - кодировщик паролей
- **AuthenticationManager** - менеджер аутентификации

#### В проекте:

```java
// CustomUserDetailsService.java - загрузка пользователей из БД
@Service
public class CustomUserDetailsService implements UserDetailsService {
    @Override
    public UserDetails loadUserByUsername(String username) {
        // Загрузка пользователя из БД и преобразование в UserDetails
    }
}
```

### 2. Авторизация (Authorization)

**Авторизация** - это процесс определения прав доступа (что вы можете делать?).

#### Типы авторизации:

1. **URL-based авторизация** - в `SecurityConfig`
2. **Метод-уровневая авторизация** - через аннотации `@PreAuthorize`
3. **Авторизация на основе ролей** - `hasRole()`, `hasAnyRole()`

#### Примеры в проекте:

```java
// URL-based
.requestMatchers("/admin/**").hasRole("ADMIN")

// Метод-уровневая
@PreAuthorize("hasRole('ADMIN')")
public String adminMethod() { ... }
```

### 3. SecurityFilterChain

**SecurityFilterChain** - цепочка фильтров безопасности, которая обрабатывает каждый HTTP запрос.

#### Основные настройки:

- **authorizeHttpRequests** - правила доступа к URL
- **formLogin** - настройка формы входа
- **logout** - настройка выхода
- **sessionManagement** - управление сессиями
- **csrf** - защита от CSRF атак

---

## 🏗️ Архитектура проекта

```
src/main/java/com/example/demo/
├── config/
│   ├── SecurityConfig.java          # Основная конфигурация Spring Security
│   ├── WebConfig.java                # Конфигурация Web MVC
│   └── DataInitializer.java          # Инициализация тестовых данных
├── controller/
│   ├── PublicController.java         # Публичные эндпоинты
│   ├── UserController.java           # Эндпоинты для пользователей
│   ├── AdminController.java          # Эндпоинты для администраторов
│   ├── ApiController.java            # REST API эндпоинты
│   ├── AuthController.java           # Регистрация и аутентификация
│   └── AuthJwtController.java        # JWT аутентификация
├── dto/
│   ├── LoginRequest.java             # DTO для запроса логина
│   └── JwtResponse.java              # DTO для ответа с JWT токеном
├── entity/
│   ├── User.java                     # Сущность пользователя
│   └── Role.java                     # Сущность роли
├── repository/
│   ├── UserRepository.java           # Репозиторий пользователей
│   └── RoleRepository.java           # Репозиторий ролей
├── security/
│   ├── JwtTokenProvider.java         # Генерация и валидация JWT токенов
│   └── JwtAuthenticationFilter.java  # Фильтр для обработки JWT токенов
└── service/
    └── CustomUserDetailsService.java # Сервис для загрузки пользователей
```

---

## 🔧 Компоненты и их назначение

### SecurityConfig.java

**Основная конфигурация Spring Security.** Демонстрирует:

1. **SecurityFilterChain** - настройка цепочки фильтров
2. **Авторизация URL** - правила доступа к различным эндпоинтам
3. **Форма логина** - кастомная страница входа
4. **Logout** - настройка выхода из системы
5. **Сессии** - управление сессиями пользователей
6. **CSRF** - настройка защиты от CSRF атак

**Ключевые моменты:**

```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity(prePostEnabled = true) // Включает @PreAuthorize
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/public/**").permitAll()      // Публичный доступ
                .requestMatchers("/user/**").authenticated()     // Требуется вход
                .requestMatchers("/admin/**").hasRole("ADMIN")  // Только админы
            )
            .formLogin(...)
            .logout(...);
    }
}
```

### CustomUserDetailsService.java

**Сервис для загрузки пользователей из базы данных.**

**Что делает:**
- Реализует интерфейс `UserDetailsService`
- Загружает пользователя по имени из БД
- Преобразует роли в `GrantedAuthority`
- Возвращает объект `UserDetails` для Spring Security

**Важно:**
- Spring Security автоматически использует этот сервис для аутентификации
- Пароль должен быть закодирован через `PasswordEncoder`

### User.java и Role.java

**Сущности для хранения пользователей и ролей в БД.**

**Связь Many-to-Many:**
- Один пользователь может иметь несколько ролей
- Одна роль может быть у нескольких пользователей
- Связь через промежуточную таблицу `user_roles`

### PasswordEncoder

**Кодировщик паролей (BCrypt).**

**Зачем нужен:**
- Пароли никогда не хранятся в открытом виде
- Используется одностороннее хеширование
- BCrypt - один из самых безопасных алгоритмов

**Использование:**
```java
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder(12);
}

// При сохранении пароля
user.setPassword(passwordEncoder.encode("plainPassword"));
```

---

## 🔐 Методы аутентификации

### 1. In-Memory Authentication

**Хранение пользователей в памяти приложения.**

**Где используется:**
- `SecurityConfig.inMemoryUserDetailsService()` - создает пользователей в памяти
- Полезно для тестирования и простых приложений

**Пример:**
```java
UserDetails admin = User.builder()
    .username("admin")
    .password(passwordEncoder.encode("admin123"))
    .roles("ADMIN")
    .build();
```

### 2. Database Authentication (JPA)

**Хранение пользователей в базе данных.**

**Где используется:**
- `CustomUserDetailsService` - загружает пользователей из БД
- `UserRepository` - доступ к данным пользователей
- Используется по умолчанию в проекте

**Преимущества:**
- Масштабируемость
- Персистентность данных
- Гибкость в управлении пользователями

### 3. JWT Token Authentication

**JWT (JSON Web Token) - stateless аутентификация для REST API.**

**Что такое JWT?**
- JWT - это стандарт для создания токенов доступа
- Токен содержит информацию о пользователе (username, роли)
- Токен подписан секретным ключом для защиты от подделки
- Stateless - серверу не нужно хранить сессии

**Структура JWT:**
1. **Header** - алгоритм и тип токена
2. **Payload** - данные (claims): username, роли, время истечения
3. **Signature** - подпись для проверки подлинности

**Где используется в проекте:**
- `JwtTokenProvider` - генерация и валидация токенов
- `JwtAuthenticationFilter` - фильтр для обработки токенов в запросах
- `AuthJwtController` - эндпоинт для получения токена

**Как работает:**

1. **Получение токена:**
   ```bash
   POST /api/auth/login
   {
     "username": "admin",
     "password": "admin123"
   }
   
   Ответ:
   {
     "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
     "type": "Bearer",
     "username": "admin",
     "authorities": "ROLE_ADMIN,ROLE_USER"
   }
   ```

2. **Использование токена:**
   ```bash
   GET /api/user/info
   Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

**Компоненты JWT в проекте:**

- **JwtConfig** - конфигурация JwtEncoder и JwtDecoder используя Spring Security OAuth2 JOSE
- **JwtTokenProvider** - генерирует токены через JwtEncoder, валидирует через JwtDecoder, извлекает данные
- **JwtAuthenticationFilter** - проверяет токен в каждом запросе, устанавливает Authentication
- **AuthJwtController** - эндпоинт `/api/auth/login` для получения токена

**Используемые библиотеки:**
- `spring-security-oauth2-jose` - официальная библиотека Spring Security для работы с JWT
- `spring-security-oauth2-resource-server` - поддержка OAuth2 Resource Server
- `NimbusJwtEncoder` и `NimbusJwtDecoder` - реализации через Nimbus JOSE + JWT SDK

**Преимущества JWT:**
- ✅ Stateless - не нужны сессии на сервере
- ✅ Масштабируемость - легко работать с несколькими серверами
- ✅ Мобильные приложения - идеально для REST API
- ✅ Микросервисы - токен можно передавать между сервисами

**Недостатки JWT:**
- ❌ Невозможно отозвать токен до истечения срока (нужен blacklist)
- ❌ Токен хранится на клиенте (риск XSS атак)
- ❌ Больший размер чем session ID

**Конфигурация:**
```properties
# application.properties
jwt.secret=mySecretKeyForJWTTokenGenerationAndValidationInSpringSecurityDemoApplication2024
jwt.expiration=86400000  # 24 часа в миллисекундах
```

---

## 🛡️ Авторизация

### 1. URL-based авторизация

**Настройка в SecurityConfig:**

```java
.authorizeHttpRequests(auth -> auth
    .requestMatchers("/public/**").permitAll()           // Всем доступно
    .requestMatchers("/user/**").authenticated()         // Требуется вход
    .requestMatchers("/admin/**").hasRole("ADMIN")        // Только админы
    .requestMatchers("/api/user/**").hasAnyRole("USER", "ADMIN") // Любая из ролей
    .anyRequest().authenticated()                         // Остальное требует входа
)
```

### 2. Метод-уровневая авторизация

**Использование аннотаций:**

```java
@PreAuthorize("hasRole('ADMIN')")
public String adminMethod() { ... }

@PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
public String superAdminMethod() { ... }

@PreAuthorize("hasRole('USER')")
public String userMethod() { ... }
```

**Важно:**
- Требует `@EnableMethodSecurity(prePostEnabled = true)` в конфигурации
- Работает на уровне методов, а не URL

### 3. Проверка ролей в Thymeleaf

**В HTML шаблонах:**

```html
<div sec:authorize="hasRole('ADMIN')">
    <!-- Видно только администраторам -->
</div>

<div sec:authorize="isAuthenticated()">
    <!-- Видно только аутентифицированным -->
</div>
```

---

## 🚀 Запуск проекта

### Требования

- Java 25 (или выше)
- Maven 3.6+ (или используйте встроенный `mvnw`)

### Шаги запуска

1. **Клонируйте репозиторий** (если нужно)

2. **Запустите приложение:**
   ```bash
   ./mvnw spring-boot:run
   ```
   или
   ```bash
   mvn spring-boot:run
   ```

3. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

4. **H2 Console** (для просмотра БД):
   ```
   http://localhost:8080/h2-console
   ```
   - JDBC URL: `jdbc:h2:mem:securitydb`
   - Username: `sa`
   - Password: (пусто)

---

## 👤 Тестовые учетные записи

При запуске приложения автоматически создаются следующие пользователи:

| Username | Password | Роли | Доступ |
|----------|----------|------|--------|
| `admin` | `admin123` | ADMIN, USER | Все страницы |
| `user` | `user123` | USER | Только пользовательские страницы |

**Или зарегистрируйте нового пользователя:**
- Перейдите на `/register`
- Создайте учетную запись (по умолчанию получит роль USER)

---

## 📚 Часто задаваемые вопросы

### 1. Что такое Spring Security?

**Spring Security** - это фреймворк для обеспечения безопасности в Spring приложениях. Он предоставляет:
- Аутентификацию (проверка личности)
- Авторизацию (проверка прав доступа)
- Защиту от различных атак (CSRF, XSS и др.)

### 2. В чем разница между Authentication и Authorization?

- **Authentication (Аутентификация)** - "Кто вы?" - проверка личности пользователя (логин/пароль)
- **Authorization (Авторизация)** - "Что вы можете делать?" - проверка прав доступа к ресурсам

### 3. Что такое UserDetailsService?

**UserDetailsService** - это интерфейс Spring Security для загрузки пользовательских данных. Он содержит один метод:
```java
UserDetails loadUserByUsername(String username)
```
Spring Security использует его для получения информации о пользователе во время аутентификации.

### 4. Зачем нужен PasswordEncoder?

**PasswordEncoder** кодирует пароли перед сохранением в БД. Это критически важно для безопасности:
- Пароли никогда не хранятся в открытом виде
- Даже администратор БД не может увидеть реальные пароли
- Используется одностороннее хеширование (нельзя расшифровать)

### 5. Что такое SecurityFilterChain?

**SecurityFilterChain** - это цепочка фильтров, которая обрабатывает каждый HTTP запрос перед тем, как он достигнет контроллера. Фильтры проверяют:
- Аутентификацию пользователя
- Авторизацию (права доступа)
- CSRF токены
- И другие аспекты безопасности

### 6. Как работает авторизация на основе ролей?

1. Пользователь имеет одну или несколько ролей (USER, ADMIN и т.д.)
2. Роли хранятся в БД и связаны с пользователем через Many-to-Many
3. При аутентификации роли преобразуются в `GrantedAuthority`
4. Spring Security проверяет роли при доступе к защищенным ресурсам

### 7. Что такое @PreAuthorize?

**@PreAuthorize** - аннотация для метод-уровневой авторизации. Она проверяет права доступа перед выполнением метода:
```java
@PreAuthorize("hasRole('ADMIN')")
public String adminMethod() { ... }
```
Если у пользователя нет нужной роли, метод не выполнится, и будет выброшено исключение `AccessDeniedException`.

### 8. Как работает CSRF защита?

**CSRF (Cross-Site Request Forgery)** - атака, при которой злоумышленник заставляет пользователя выполнить нежелательные действия.

Spring Security защищает от CSRF:
- Генерирует уникальный токен для каждой сессии
- Требует этот токен в POST/PUT/DELETE запросах
- Проверяет токен перед обработкой запроса

**В проекте CSRF отключен для упрощения демонстрации**, но в продакшене его нужно включить!

### 9. Как управляются сессии?

В `SecurityConfig` настроено:
```java
.sessionManagement(session -> session
    .sessionCreationPolicy(SessionCreationPolicy.IF_REQUIRED)
    .maximumSessions(1)
    .maxSessionsPreventsLogin(false)
)
```

- **IF_REQUIRED** - сессия создается при необходимости
- **maximumSessions(1)** - максимум 1 активная сессия на пользователя
- **maxSessionsPreventsLogin(false)** - новая сессия закрывает старую

### 10. Как добавить новую роль?

1. Создайте роль в БД через `RoleRepository`
2. Назначьте роль пользователю через `UserRepository`
3. Обновите правила авторизации в `SecurityConfig` или используйте `@PreAuthorize`

### 11. Как работает CustomUserDetailsService?

1. Пользователь вводит логин/пароль
2. Spring Security вызывает `loadUserByUsername(username)`
3. Сервис загружает пользователя из БД
4. Преобразует роли в `GrantedAuthority`
5. Возвращает `UserDetails` объект
6. Spring Security проверяет пароль через `PasswordEncoder`
7. Если все верно - пользователь аутентифицирован

### 12. В чем разница между hasRole() и hasAuthority()?

- **hasRole("ADMIN")** - автоматически добавляет префикс "ROLE_", ищет "ROLE_ADMIN"
- **hasAuthority("ROLE_ADMIN")** - ищет точное совпадение "ROLE_ADMIN"

В проекте используется `hasRole()`, так как роли хранятся с префиксом "ROLE_".

### 13. Как защитить REST API?

В проекте демонстрируется защита REST API:
- Использование `@RestController` вместо `@Controller`
- Аннотации `@PreAuthorize` для API методов
- Возврат JSON вместо HTML
- **JWT токены для stateless аутентификации**

Пример:
```java
@GetMapping("/api/user/info")
@PreAuthorize("hasAnyRole('USER', 'ADMIN')")
public ResponseEntity<Map<String, Object>> userInfo() { ... }
```

### 14. Как работает JWT аутентификация?

**JWT (JSON Web Token)** - это стандарт для создания токенов доступа.

**Используемые библиотеки:**
Проект использует **официальные библиотеки Spring Security** для работы с JWT:
- `spring-security-oauth2-jose` - поддержка JOSE (JSON Object Signing and Encryption)
- `spring-security-oauth2-resource-server` - поддержка OAuth2 Resource Server
- `NimbusJwtEncoder` и `NimbusJwtDecoder` - реализации через Nimbus JOSE + JWT SDK

**Процесс работы:**

1. **Получение токена:**
   - Клиент отправляет POST запрос на `/api/auth/login` с username и password
   - Сервер проверяет учетные данные через `AuthenticationManager`
   - Если валидны - генерируется JWT токен через `JwtTokenProvider` используя `JwtEncoder`
   - Токен возвращается клиенту

2. **Использование токена:**
   - Клиент отправляет запросы с заголовком `Authorization: Bearer <token>`
   - `JwtAuthenticationFilter` извлекает токен из заголовка
   - Проверяет валидность токена через `JwtDecoder`
   - Извлекает username и роли из токена
   - Создает объект `Authentication` и устанавливает в `SecurityContext`

3. **Валидация токена:**
   - Проверяется подпись токена через `JwtDecoder` (защита от подделки)
   - Проверяется срок действия токена
   - Извлекаются данные пользователя из claims токена

**Пример использования:**
```bash
# 1. Получение токена
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# Ответ:
# {"token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...","type":"Bearer","username":"admin"}

# 2. Использование токена
curl http://localhost:8080/api/user/info \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

### 15. В чем разница между JWT и Session-based аутентификацией?

| Аспект | JWT | Session-based |
|--------|-----|---------------|
| **Хранение** | Токен на клиенте | Session ID на клиенте, данные на сервере |
| **Stateless** | Да (stateless) | Нет (stateful) |
| **Масштабируемость** | Легко масштабировать | Нужна общая сессия (Redis, DB) |
| **Отзыв токена** | Сложно (нужен blacklist) | Легко (удалить сессию) |
| **Размер** | Больше (содержит данные) | Меньше (только ID) |
| **Безопасность** | Риск XSS (токен в localStorage) | Риск CSRF (нужна защита) |
| **Использование** | REST API, микросервисы | Web приложения с формами |

**В проекте поддерживаются оба подхода:**
- **Session-based** - для веб-страниц (форма логина)
- **JWT** - для REST API (`/api/**`)

### 16. Как работает JwtAuthenticationFilter?

`JwtAuthenticationFilter` - это фильтр Spring Security, который:

1. **Извлекает токен** из заголовка `Authorization: Bearer <token>`
2. **Валидирует токен** через `JwtTokenProvider`
3. **Извлекает данные** пользователя (username, роли) из токена
4. **Создает Authentication** объект
5. **Устанавливает в SecurityContext** - теперь Spring Security знает о пользователе

Фильтр выполняется **перед** `UsernamePasswordAuthenticationFilter`, поэтому:
- Если есть валидный JWT токен - используется он
- Если нет токена - используется форма логина

### 17. Как настроить время жизни JWT токена?

В `application.properties`:
```properties
jwt.expiration=86400000  # 24 часа в миллисекундах
```

Или в коде:
```java
@Value("${jwt.expiration}")
private long jwtExpirationMs;
```

**Рекомендации:**
- Access token: 15 минут - 1 час
- Refresh token: 7-30 дней (если используется)

### 18. Как работает Many-to-Many связь User-Role?

- Таблица `users` - пользователи
- Таблица `roles` - роли
- Таблица `user_roles` - промежуточная таблица связи

Один пользователь → много ролей
Одна роль → много пользователей

### 19. Что такое GrantedAuthority?

**GrantedAuthority** - интерфейс, представляющий право доступа. В Spring Security роли реализуются как `GrantedAuthority` с префиксом "ROLE_".

Пример: роль "ADMIN" становится "ROLE_ADMIN" как `GrantedAuthority`.

---

## 📖 Дополнительные ресурсы

- [Официальная документация Spring Security](https://spring.io/projects/spring-security)
- [Spring Security Reference](https://docs.spring.io/spring-security/reference/index.html)
- [BCrypt Password Encoding](https://en.wikipedia.org/wiki/Bcrypt)
- [JWT.io - Декодер и информация о JWT](https://jwt.io/)
- [RFC 7519 - JSON Web Token (JWT)](https://tools.ietf.org/html/rfc7519)

---

## 🎓 Что демонстрирует этот проект

✅ **Аутентификация:**
- In-memory аутентификация
- Database аутентификация через JPA
- **JWT токены для stateless аутентификации**
- Custom UserDetailsService
- Password encoding (BCrypt)

✅ **Авторизация:**
- URL-based авторизация
- Метод-уровневая авторизация (@PreAuthorize)
- Role-based access control (RBAC)
- Проверка ролей в Thymeleaf

✅ **Безопасность:**
- CSRF защита (настройка)
- Session management
- Access denied handling
- Secure logout

✅ **Интеграция:**
- Spring Data JPA
- Thymeleaf с Spring Security
- REST API защита
- **JWT аутентификация для REST API (Spring Security OAuth2 JOSE)**
- H2 Database

---

## 📝 Структура эндпоинтов

| URL | Доступ | Описание |
|-----|--------|----------|
| `/` | Все | Главная страница |
| `/login` | Все | Страница входа |
| `/register` | Все | Регистрация |
| `/public/**` | Все | Публичные эндпоинты |
| `/user/dashboard` | Аутентифицированные | Dashboard пользователя |
| `/user/profile` | USER | Профиль пользователя |
| `/user/settings` | USER | Настройки (метод-уровневая) |
| `/admin/panel` | ADMIN | Админ панель |
| `/admin/users` | ADMIN | Управление пользователями |
| `/admin/settings` | ADMIN, SUPER_ADMIN | Настройки системы |
| `/api/public` | Все | Публичный API |
| `/api/auth/login` | Все | Получение JWT токена (POST) |
| `/api/auth/validate` | Аутентифицированные | Проверка валидности токена |
| `/api/user/info` | USER, ADMIN | API пользователя (JWT или Session) |
| `/api/admin/stats` | ADMIN | Статистика (API, JWT или Session) |
| `/h2-console` | Все | H2 Database Console |

---

## 🔍 Отладка и тестирование

### Просмотр БД через H2 Console

1. Откройте `http://localhost:8080/h2-console`
2. Введите:
   - JDBC URL: `jdbc:h2:mem:securitydb`
   - Username: `sa`
   - Password: (оставьте пустым)
3. Выполните SQL запросы:
   ```sql
   SELECT * FROM users;
   SELECT * FROM roles;
   SELECT * FROM user_roles;
   ```

### Тестирование API

**Публичный API:**
```bash
curl http://localhost:8080/api/public
```

**JWT аутентификация:**

1. **Получение JWT токена:**
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

Ответ:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "type": "Bearer",
  "username": "admin",
  "authorities": "ROLE_ADMIN,ROLE_USER"
}
```

2. **Использование JWT токена:**
```bash
# Сохраните токен в переменную
TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Используйте токен в заголовке Authorization
curl http://localhost:8080/api/user/info \
  -H "Authorization: Bearer $TOKEN"
```

3. **Проверка валидности токена:**
```bash
curl -X POST http://localhost:8080/api/auth/validate \
  -H "Authorization: Bearer $TOKEN"
```

**Session-based аутентификация (для веб-страниц):**
```bash
# Сначала получите сессию через форму логина в браузере
# Затем используйте cookies для запросов
```

---

## ⚠️ Важные замечания

1. **CSRF отключен** - в продакшене обязательно включите!
2. **H2 in-memory** - данные теряются при перезапуске
3. **Тестовые пароли** - в продакшене используйте сложные пароли
4. **H2 Console доступна всем** - в продакшене ограничьте доступ

---

## 📞 Поддержка

Если у вас есть вопросы о Spring Security, которые не покрыты в этом README, изучите код проекта - каждая часть содержит подробные комментарии, объясняющие концепции Spring Security.

---

**Удачного изучения Spring Security! 🚀**
